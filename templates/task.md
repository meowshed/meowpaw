---
id: TSK-NNNN
artifact: task
status: draft # draft | approved — in-progress and done derive
revised: YYYY-MM-DD
epic: EPC-NNNN
closes: [R-AREA-nnn]
issue:
---

# <What this task does>

One task, one branch, one pull request, one review ([R-H-133]).

## What to do

Enough that an implementer with no access to the conversation can do it
([R-H-042]). The constraints that bind it, verbatim. The interfaces it touches.

## Depends on

Other tasks that must close first, **and why** — a dependency that is only about
convenience is not a dependency.

## Evidence

The command, its result and the revision, once done ([R-H-070]). Where the
output is a document rather than code, the artifact it produced.

## Left alone

What documentation was deliberately not updated, and why ([R-H-047]).
