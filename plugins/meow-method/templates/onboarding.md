---
id: onboarding
artifact: onboarding
status: draft # draft, then approved; superseded by a later onboarding
revised: YYYY-MM-DD
---

<!-- Written to the writing standard meow-prose ships: lead with what was found, give each figure its source, and state a gap as plainly as a finding. -->

# Onboarding <the repository's name>

What this repository already is, read from its documentation, its harness and
its code, and what couldn't be determined. It records no requirement and no
decision, because code shows what a system does and never what it must do.

## Verbs

Which verbs resolve and which don't, from `meow-verbs status` where that unit
is installed, first, because how a repository is checked is the first thing a
person hits.

## Conventions

Each convention observed, as how often it appears, offered as a decision for a
person to take, never asserted as a rule: for example, "commit subjects: 212
of 240 follow one form; adopt it?"

## Documents

Every document the repository has outside the record, each with exactly one
outcome: migrated to a named artifact, cited as a source, superseded by a named
artifact, or discarded with the reason. Nothing is deleted before its row is
approved.

| Document | Outcome                                    | Where, or why                 |
| -------- | ------------------------------------------ | ----------------------------- |
| `<path>` | <migrated, cited, superseded or discarded> | <the artifact, or the reason> |

## Gaps

What couldn't be determined, each as a question a person can answer, because a
gap left out reads as something known.

## Adoption

The steps that bring the repository into the method, each leaving it working:

1. <The first step, and what works once it is done>.
