# SPDX-FileCopyrightText: 2026 Andrew Vasilyev <me@retran.me>
# SPDX-License-Identifier: Apache-2.0

# What this unit may load on every turn, in characters (REQ-1056). The gate is
# a prompt hook, which loads only when a publishing command fires it, so
# nothing of it sits in context on a turn. Measured at 0 by
# tools/check_budget.py on 2026-09-23.
permanent_characters = 0
